# gateway/pyproject.toml additions (or pytest.ini equivalent)
# Add to your existing pyproject.toml under [tool.pytest.ini_options]:
#
# [tool.pytest.ini_options]
# asyncio_mode = "auto"
# testpaths = ["tests"]
# addopts = "--cov=app --cov-report=term-missing --cov-fail-under=60"
