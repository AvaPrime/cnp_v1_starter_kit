# CNP Gateway — Python dependencies
# Managed by: gateway/requirements.txt
# Lock file: generate with `pip-compile requirements.in` (Phase 2 migration to pyproject.toml)
#
# AUDIT FIX (P0-01): pytest-asyncio==1.1.0 does not exist on PyPI.
# Was causing pytest exit code 4 (collection error) — tests never ran.
# Fixed to >=0.23,<1.0 which is the current stable series.

# ── Runtime ──────────────────────────────────────────────────────────────────
fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
aiosqlite==0.21.0

# asyncio-mqtt is deprecated upstream; aiomqtt is the maintained fork.
# Keeping asyncio-mqtt for now to avoid API churn — migrate in Phase 1 (P1-06).
asyncio-mqtt==0.16.2
# aiomqtt>=2.0.0  # <-- Phase 1 migration target

orjson==3.11.1
httpx==0.28.1

# ── Development / Testing ─────────────────────────────────────────────────────
pytest==8.4.1
pytest-asyncio>=0.23,<1.0      # FIXED: was ==1.1.0 which does not exist
pytest-cov>=4.1.0
httpx>=0.28.1                   # used by AsyncClient in conftest

# ── Security scanning (used in CI) ───────────────────────────────────────────
bandit>=1.7.0
pip-audit>=2.7.0

# ── Linting / type checking (used in CI) ─────────────────────────────────────
ruff>=0.4.0
mypy>=1.10.0
